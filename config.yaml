name: omodel
channels:
  - defaults
  - conda-forge
dependencies:
  - pip
  - opencv

  - pip:
    - pip
    - gpustat
    - setuptools
    - hyperopt
    - ray[tune]
    - ipdb
    - wheel
    - Pillow
    - matplotlib
    - numpy==1.22.4
    - gym[atari]==0.23.0

    - pyyaml
    - scikit-learn




# conda env create -f cpu.yaml
# conda env update --file cpu.yaml --prune
# conda remove --name omodel --all
